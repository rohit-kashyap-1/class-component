import React, { Component } from 'react'

export default class About extends Component {
  render() {
    return (
      <div>
        <h1>About us</h1>
        <p>It is a lifecycle method called automatically by React after the component has been rendered for the first clip and inserted into the DOM. The componentDidMount() method's main purpose is to perform any setup tasks or face effects required for the element to work decently.</p>
      </div>
    )
  }
}
